# serveurMachine

```git clone https://github.com/cygnauh/serveurMachine.git```

```cd serveurMachine```

```npm install ```

to run the app
```node .```
then use http://localhost:5000 to access
##Method
